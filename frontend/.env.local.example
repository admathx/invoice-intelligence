NEXT_PUBLIC_API_BASE=http://localhost:8000
# v0 has no auth (see SPEC.md §1 out-of-scope). Point the dashboard at one tenant
# by id until Phase 5+ adds a session. Get an id from `make smoke` output or the DB.
NEXT_PUBLIC_DEV_TENANT_ID=
