DATABASE_URL=postgresql+psycopg://invoice:invoice@localhost:5432/invoice_intelligence
REDIS_URL=redis://localhost:6379/0
ANTHROPIC_API_KEY=
# UPLOAD_DIR intentionally omitted — defaults to an absolute path anchored to
# backend/, which is correct regardless of the process's cwd. Only set this if
# you specifically want uploads stored somewhere else.
